pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'start build'
      }
    }

    stage('upload') {
      steps {
        echo 'start upload'
      }
    }

    stage('deploy') {
      steps {
        echo 'start deploy'
      }
    }

  }
}